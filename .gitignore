# Tudo que o "composer install" reconstroi sozinho a partir do composer.lock
# (core, profile Arizona Quickstart, modulos/temas/bibliotecas contrib) fica
# de fora do git - so o codigo proprio (web/modules/custom) e a config vao.
/vendor/
/web/core/
/web/profiles/custom/az_quickstart/
/web/modules/contrib/
/web/themes/contrib/
/web/libraries/
/drush/Commands/contrib/

# Gerado pelo composer scaffold (drupal-scaffold) a cada install.
/web/autoload.php
/web/index.php
/web/robots.txt
/web/update.php
/web/.htaccess
/web/sites/default/default.services.yml
/web/sites/default/default.settings.php

# Gerado pelo entrypoint do container a cada boot - nunca vai pro git
# (ver docker/entrypoint.sh e o include em settings.php de cada site).
# Padrao com wildcard: cobre "default", "dfis", "demat" e qualquer site novo
# que for criado depois (ver scripts/az_provisionar_esqueleto.php), sem
# precisar editar este arquivo a cada site novo.
/web/sites/*/settings.local.php

# Cache (derivativos de imagem, CSS/JS agregado, sitemap) - o Drupal
# regenera tudo isso sozinho. O sitemap em especial: um site rodando
# diverge do commit toda vez que o cron roda, travando "git pull" com
# conflito se ficar versionado.
/web/sites/*/files/styles/
/web/sites/*/files/php/
/web/sites/*/files/css/
/web/sites/*/files/js/
/web/sites/*/files/xmlsitemap/
# Cache de tradução JS (nome com hash, muda toda vez que uma string de
# interface e traduzida/atualizada) - mesmo problema do sitemap.
/web/sites/*/files/languages/

# DDEV (ferramenta so de desenvolvimento local, nao entra na imagem de producao).
/.ddev/

# Copia crua do repositorio do az_quickstart (upstream) que ficou solta na raiz
# do projeto por causa do bootstrap inicial via DDEV - nao e usada pelo site
# (o docroot real e "web/", ver .ddev/config.quickstart.yaml). Achado ao
# preparar o repo para o GitHub; mantido fora do git so por seguranca, o ideal
# e apagar essas pastas do disco quando sobrar tempo.
/az_quickstart.info.yml
/az_quickstart.install
/az_quickstart.links.menu.yml
/az_quickstart.post_update.php
/az_quickstart.profile
/modules/
/themes/
/assets/
/recipes/
/test/
/libraries/
/drush/
/.yarn/
/.yarnrc.yml
/.tugboat/
/.lando.yml
/.probo.yaml
/CONTRIBUTING.md
/RELEASES.md
/LICENSE.txt
/COPYRIGHT.txt
/package.json
/yarn.lock
/phpcs.xml.dist
/phpstan.neon
/phpstan-contrib.neon
/phpunit.xml.dist
/quickstart_branch.sh
/index.html
/index.nginx-debian.html

node_modules/
*.log
.DS_Store
.env
!.env.example
